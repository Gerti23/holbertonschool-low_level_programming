#include "main.h"
/**
 *  int _abs - computes the absolute value of an integer.
 * @n: the number to check
 *
 * Return number if number is positive
 * Return -number if number is negative
 */
int _abs(int number)
{
	if (number > 0)
		return ('number');
	else 
	return ('-number');
}	

	